export const metadata = {
    title: 'Invotrack | Authentication',
    description: 'Invotrack - Invoice Management System',
  };
  
  export default function AuthLayout({ children }) {
    return children;
  }