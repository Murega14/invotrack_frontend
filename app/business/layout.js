export const metadata = {
    title: 'Invotrack | Business',
    description: 'Invotrack - Invoice Management System'
};

export default function BusinessLayout ({ children }) {
    return children;
}