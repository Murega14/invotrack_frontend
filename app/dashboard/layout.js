export const metadata = {
    title: 'Invotrack | Dashboard',
    description: 'Invotrack - Invoice Management System'
};

export default function DashboardLayout({ children }) {
    return children;
}